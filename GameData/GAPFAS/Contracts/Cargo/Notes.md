// Notes
//--------------

1. Cargo retrieveal from Site (Index > 0) moved to recovery site (defined in Groups.cfg cannot be the retrieval site or index 0) - Load Master (LM) kerbal required
2. Supply Drop a probe to remote land Location (100-200 km from Index > 0) - requires part validation on dropped probe
3. Recover at the Index airport selected or the KSC (site 0)


